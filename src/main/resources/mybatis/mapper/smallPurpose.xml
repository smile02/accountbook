<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="smallPurpose">
	<select id="smallPurposeList" resultType="smallPurpose" parameterType="String">
		select * from small_purpose where big_name = #{value}
	</select>	
	
</mapper>